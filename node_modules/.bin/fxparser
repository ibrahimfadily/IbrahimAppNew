#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@react-native-community/cli-doctor/node_modules/@react-native-community/cli-platform-android/node_modules/fast-xml-parser/src/cli/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../@react-native-community/cli-doctor/node_modules/@react-native-community/cli-platform-android/node_modules/fast-xml-parser/src/cli/cli.js" "$@"
  ret=$?
fi
exit $ret
